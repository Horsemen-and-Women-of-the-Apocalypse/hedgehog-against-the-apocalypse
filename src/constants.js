const TILE_SIZE_PX = 32

// [width, height]
const MAP_SIZE = {
    width: 40,
    height: 25
};

const CITY_HEADSTART = 2;

const SCROLL_SPEED = 1;

export {
    TILE_SIZE_PX,
    MAP_SIZE,
    SCROLL_SPEED,
    CITY_HEADSTART
}
